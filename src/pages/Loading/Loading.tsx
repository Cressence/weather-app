import React from 'react';

import Wrapper from './../../components/Wrapper';
import './loading.scss';

const Loading = ( ) => {
    return (
        <Wrapper>
            <p>Loading...</p>
        </Wrapper>
    );
}

export default Loading;